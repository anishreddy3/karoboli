[build]
builder = "railpack"

[deploy]
startCommand = "uvicorn karoboli_gateway.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
